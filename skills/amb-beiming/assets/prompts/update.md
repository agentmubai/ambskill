# 增量更新：任务「${task}」（领域：${domain}）

工程目录：`${project}`。相对路径都相对它。新料已萃取、合并并重建了任务池；你的工作是把新原子对照进现有知识包与技能，只改需要改的地方。

## 读

1. `${pack_dir}/methods.md`、`layers.md`、`cards.md`（现有知识包）。
2. `${pool_dir}/atoms.jsonl` 与 `units.jsonl` 里本次新增的原子（下面列了 id）及其所属单元。
3. `${skill_dir_out}/SKILL.md`（现有技能）。
4. `${skill_dir}/references/09-update.md` 规则节（覆盖对照三分、冲突表格式、回滚条件）。

新增原子：${new_ids}

## 写 `${compare_out}`（覆盖对照）

把每条新原子归入三类之一：

| 新原子 id | 归类 | 对应模块 / 新模块名 | 说明 |
|---|---|---|---|
| … | 已覆盖（现有模块已有同义主张，只补依据 id） | M?? | |
| … | 新增（现有模块没有，值得成为新模块或新分支） | M??（新） | |
| … | 冲突（与现有主张相反或条件矛盾） | M?? | 写进冲突表 |

## 写 `${conflict_out}`（冲突表，追加不覆盖）

| 日期 | 任务 | 模块 | 旧主张（原子 id） | 新主张（原子 id） | 作者是否说明了变化 | 处理 |
|---|---|---|---|---|---|---|

处理只能是：以新为准（作者自己说明了变化）/ 以旧为准（新料是举例或 low）/ 并存为分支（条件不同）/ 交使用者决定。作者自己没说明变化的，默认交使用者；"新料更晚"不是以新为准的理由。这张表只写本任务，别的任务有各自的表。

## 应用改动

只改知识包 `${pack_dir}/` 与草稿 SKILL.md，不改草稿的 `references/`（改完由执行 agent 用 `compose --sync-refs` 从知识包复制过去，保证两边一致）：

- methods.md：已覆盖的只补依据 id；新增的加模块（编号接在末尾，不重排旧号）；冲突按处理结果改，"交使用者"的行先不改。
- layers.md：新增的道 / 术 / 势 从新原子 original 逐字取；不改旧条目的原话。
- cards.md：新单元有对照关系的补卡。
- `${skill_dir_out}/SKILL.md`：只改受影响的 Phase 与"本任务不判的问题"；不重写全文；点名新模块号。

## 写 `${report_out}`

三类各多少条；改了哪些文件的哪些节；冲突表新增几行、几行交使用者。

改完执行 agent 会重跑 六层逐字核对 → 同步 references → 成品体检 → 常备题集回归；回归掉到 WEAK 会回滚你的改动，所以每处改动都要能说出"为哪条新原子而改"。
